using TMPro;
using UnityEngine;

public class BattleMenu : MonoBehaviour
{
    //Player attributes
    private gameManager gameManager;

    private TextMeshProUGUI health;
    private TextMeshProUGUI tolerance;
    private TextMeshProUGUI stamina;

    //Enemy Attributes
    //to be added

    // Start is called before the first frame update
    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }

    /**
     * @brief Change the value of a valid player/enemy field
     */
    //public void OnValueChange(, int num) {
    //    if (valueToChange.Equals("health") {
    //    }
    //}
}